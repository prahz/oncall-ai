{
  "name": "blast_radius_calculator",
  "description": "STUB (Day 4 = real dependency-graph traversal). Count distinct affected services among recent alerts to classify blast radius.",
  "type": "API",
  "code": { "$file": "code.py" },
  "permissions": { "grants": [
    { "resource_type": "datastore_table", "resource_name": "alerts",
      "permission_ids": ["datastore.table.read", "datastore.record.read"] }
  ]}
}
